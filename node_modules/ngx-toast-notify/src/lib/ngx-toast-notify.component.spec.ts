import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NgxToastNotifyComponent } from './ngx-toast-notify.component';

describe('NgxToastNotifyComponent', () => {
  let component: NgxToastNotifyComponent;
  let fixture: ComponentFixture<NgxToastNotifyComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ NgxToastNotifyComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(NgxToastNotifyComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
