import { TestBed } from '@angular/core/testing';

import { NgxToastNotifyService } from './ngx-toast-notify.service';

describe('NgxToastNotifyService', () => {
  let service: NgxToastNotifyService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(NgxToastNotifyService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
